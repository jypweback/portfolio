# Test
spring:
  profiles:
    active: test # 기본 환경 선택

# test 환경
---
spring:
  profiles: test
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    cache: false

logging:
  level:
    org:
      hibernate:
        type : trace
